-- init.lua

-- [[ Options ]]

require 'core.options'

-- [[ Basic Keymaps ]]

require 'core.keymaps'

-- [[ Basic Autocommands ]]

require 'core.autocommands'

-- [[ Install `lazy.nvim` plugin manager and its configuration ]]

require 'core.lazy'

-- The line beneath this is called `modeline`. See `:help modeline`
-- vim: ts=2 sts=2 sw=2 et
